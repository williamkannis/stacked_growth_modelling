% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth_summary_functions.R
\name{mean_ci_batch}
\alias{mean_ci_batch}
\title{Batch median and 95\% credible interval summary}
\usage{
mean_ci_batch(
  mod.df,
  mod.dir,
  parallel = F,
  mc.cores = NULL,
  params = c("mu", "tau", "beta", "sigma_length"),
  digits = 3,
  ci = c(0.025, 0.975)
)
}
\arguments{
\item{mod.df}{Data.frame containing model file names. Must have column
named "model".}

\item{mod.dir}{File path for Stan model output files}

\item{mc.cores}{Number of core for parallel processing if parallel = T.
Default is NULL.}

\item{params}{Vector containing parameters to summarize c("mu","tau","beta",
"sigma_length").}

\item{digits}{Number of digits to round values. Default is 3 digits.}

\item{ci}{Vector containing lower and upper percentiles used for credible
intervals. Default is c(0.025,0.975).}
}
\value{
Data.frame with rows for each model and a single column for each
user-specified parameter with median and 95\% CI as a character in the
following format: "median (lwr,upr)".
}
\description{
Summarizes the median and 95\% credible intervals of posterior
distribution of parameters from a set of models for use in tables.
}
\details{
Posterior distribution for select parameters are extracted from a
list of models. Posterior distributions are summarized into median and 95\%
credible interval. For each parameter median and 95CI values are formatted
into a single character string with a set number of digits.
}
